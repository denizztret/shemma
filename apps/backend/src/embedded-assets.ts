// GENERATED placeholder. Populated by scripts/build-release.sh before
// `bun build --compile` so frontend assets are embedded into the binary
// via `import ... with { type: "file" }`. Restored to empty after build.
// In dev mode this stays empty and backend falls back to reading from disk.
export const EMBEDDED_ASSETS: Record<string, string> = {};
