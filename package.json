{
  "name": "shemma-root",
  "version": "0.25.3",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently 'SHEMMA_PROFILE=dev bun run --cwd apps/backend dev' 'bun run --cwd apps/frontend dev'",
    "test": "bun --cwd packages/shemma-domain test && bun --cwd apps/backend test && bun --cwd packages/shemma-client test && bun --cwd packages/shemma-cli test && bun --cwd packages/shemma-mcp test",
    "lint": "biome check ."
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.0",
    "concurrently": "^9.0.0",
    "typescript": "^5.5.0"
  }
}
