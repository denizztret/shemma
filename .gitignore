node_modules/
dist/
*.log
.DS_Store

# tsc emit artifacts (frontend may invoke `tsc` for type-check)
apps/frontend/src/**/*.js
*.tsbuildinfo

release/
apps/backend/src/frontend-dist/

# Backlog.md (local-only task management, не коммитим)
backlog/

# Superpowers brainstorming visual companion (local-only)
.superpowers/

# Shemma local storage (per-project; smoke / accidental cwd runs)
.shemma/
# Legacy: pre-rename storage folder
.didraw/

# Shemma auth state (local user config)
.config/shemma/

# obsidian
docs/.obsidian/

# Claude Code runtime artifacts
.claude/scheduled_tasks.lock
# Local per-machine permission allowlist (personal, not shared)
.claude/settings.local.json

# Internal maintainer notes (process/policy, not public)
CLAUDE.local.md

# Spike / throwaway scripts (local-only, never committed)
scratch/

# Manual-test / dogfood screenshots — local-only history, не пушим
docs/screenshots/