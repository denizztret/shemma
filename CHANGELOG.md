## Unreleased

### Features

- **Плавный регулятор плотности раскладки вместо трёх кнопок масштаба (DRW-239).** Прежние кнопки Compact/Normal/Roomy вызывали полный пересчёт ELK (`autoElkLayout`) — flip-пасс по геометрии, рефлоу контейнеров и рост ширины боксов перетряхивали выверенную раскладку, вернуть прежний вид было трудно. Теперь в панели выделения — **ползунок «Плотность»** (0.5–2.0, центр 1.0): тащишь вправо — раздвигает выделенное, влево — сближает, **без запуска ELK**, так что направление и структура сохраняются по построению (⌘⇧L остаётся отдельным действием «перекомпоновать»). Меняются только позиции (размеры фигур не трогаем; коннекторы — bound elbow — следуют сами); контейнеры и фрейм дотягиваются по контенту — в т.ч. фрейм-предок раздвигаемого контейнера и **фрейм с закреплённым размером** (size-pin защищает от авто-раскладки, но прямой жест плотности фрейм обтягивает; он остаётся size-pinned на новом размере). Модель относительная, без «дефолта»/«нормы»: жест масштабирует снимок, снятый в момент захвата (live-превью без накопления), а откат — обычный ⌘Z (весь drag = один шаг истории). **Минимальный зазор**: при сближении коэффициент аналитически зажимается по самой тесной паре каждого уровня (равномерный scale масштабирует все попарные расстояния на один множитель), поэтому фигуры никогда не наезжают; раздвигание не ограничено. Работает для выделения / контейнера / фрейма / борда; **любые объекты-боксы** (узлы, текст, стикеры, картинки), а не только schema-узлы; выделение группируется по родителю — узлы не выпрыгивают из контейнеров; якорь — верх-левый угол для обёрток (без дрейфа) и центр для свободного выделения. Регулятор двигает и **закреплённые (pinned)** фигуры — это прямое действие пользователя (= задаёт пользовательскую позицию), а пины защищают только от AI/авто-раскладки; фигуры остаются закреплёнными на новых позициях. Board-панель «По умолчанию» сохраняет кнопки Compact/Normal/Roomy — это дефолтный spacing для нового контента / импорта / AI-агента (он не трогает уже размещённые схемы), отдельная от регулятора вещь. Чистые `computeRespreadLevel` + `levelMinK` (10 unit: spread/compress, якоря, сохранение структуры, min-gap, пины) + wiring-тест слайдера; live-verified на доске (spread/compress, экстремальное сжатие зажато без наездов, ⌘Z откатывает жест, top-level пин заморожен).

- **Кнопка «Обтянуть текст» работает по выделению / контейнеру / фрейму рекурсивно, а соседи не наезжают (DRW-232).** Прежде ⌘⇧F и кнопка обтягивали только сами выделенные geo/note: на контейнере или фрейме кнопка была видна (её видимость считалась рекурсивно по потомкам), но **не делала ничего** — её цель (`schema-container`/`frame`) не входит в обтягиваемые типы, а применение шло лишь по выделенным id. Теперь цели собираются рекурсивно (`collectDescendantIds`): выделен контейнер/фрейм → обтягиваются все вложенные текстовые узлы, **включая узлы во вложенных контейнерах**; свободное мульти-выделение → и сами узлы, и дети выделенных контейнеров. После обтяжки обёртки приводятся в порядок **изнутри наружу**: (1) **точечный сдвиг наезжающих соседей** вдоль направления контейнера/фрейма (`props.direction` / `meta.didrawDirection`; не-cardinal → ось из геометрии) — двигаются ТОЛЬКО реально наезжающие узлы до зазора ≥ `RESPREAD_MIN_GAP` (24px), остальные интервалы не трогаются (минимальное возмущение, не полная раскладка), `meta.pinned`-узлы остаются неподвижными опорами, bound-стрелки растягиваются сами; (2) **envelope-рост/сжатие родителя** под bbox детей + padding (переиспользует `refitWrapper` из DRW-239) по всей цепочке предков (узел → контейнер → вложенный контейнер → фрейм). Вся операция — одна undo-точка. Новый **force-режим** (Opt-клик по кнопке + настраиваемый шорткат **⌘⌥⇧F**, по аналогии с force-раскладкой ⌘⌥⇧L): обычный режим уважает вручную заданный размер (`didrawSizePinned`) — не ломает руками настроенные фигуры; force обтягивает всё, включая size-pinned. Чистые `resolveOverlaps1D` / `resolveOverlapsAlongFlow` / `flowAxis` (15 unit: push только наезжающих, каскад, порядок, min-gap, pinned-опора, LR/RL/TB/BT + ось из геометрии) + `shouldManualFit` (6 unit: force/size-pin/типы) + match-тесты шортката; свободное выделение без оси и вторая ось grid-фрейма — на ⌘⇧L.
- **Авто-обтяжка вложенных узлов теперь дорастает родителя, а правка текста переобтягивает (DRW-232, Фаза 2).** Событийная авто-обтяжка (DRW-228) и раньше срабатывала на узлах внутри контейнера/фрейма — её листенер фильтрует по типу (geo/note), а не по родителю; видимый «наезд и вылезание за контейнер» был не пропущенной обтяжкой, а **отсутствием envelope-роста**. Теперь: (1) **родитель дорастает под выросший узел** — `growWrappersForShapes` поднимается по цепочке обёрток изнутри наружу (узел → контейнер → фрейм) и расширяет каждую под содержимое + padding, **только рост** (max с текущим размером): уменьшение ребёнка не сжимает родителя, соседи не двигаются и не перепаковываются, `props.direction` сохраняется (контракт «AI-правка не перевыравнивает»). Срабатывает на авто-обтяжке (в том же шаге истории, что и обтяжка) и на **ручном drag-resize** ребёнка (по завершении жеста, AC #7); **user-pinned размер обёртки** (явный ресайз фрейма/контейнера) не трогается. (2) **Правка текста уже-обтянутого узла снова обтягивает его**: размер, заданный обтяжкой, помечается `meta.didrawSizeOrigin: "fit"`, а drag-resize/force-fit — `"user"`; авто-обтяжка переобтягивает только `"fit"`-узлы по завершении правки, никогда — `"user"` (раньше первый авто-фит ставил `didrawSizePinned` и блокировал любую переобтяжку). Push соседей и сжатие родителя остаются ручными (⌘⇧F/⌘⇧L). Чистые `growOnlyBox` (5 unit) + `growWrappersForShapes` на fake-editor (inner→outer рост, идемпотентность/loop-safe, grow-only без сжатия, skip user-pinned, growY, без обёртки-предка) + origin-логика `shouldAutoFit`/`computePinUpdates`.

## 0.31.1 — 2026-06-08 — Битый цвет/record больше не обнуляет доску при загрузке

Patch-фикс устойчивости загрузки доски (DRW-231).

### Fixes

- **Один битый цвет шейпа больше не обнуляет всю доску (DRW-231).** Сырой hex в `props.color` (например `#6A1B9A` из mermaid style-директивы, см. DRW-153) вместо именованной палитры tldraw проходил нестрогий backend-стор, но строгий `loadSnapshot` во фронтенде бросал `ValidationError` на этой записи и **прерывал загрузку всего снапшота** — доска открывалась пустой («пропали схемы»), хотя данные были целы. Лечение трёхслойное: (1) новый SSOT `coerceColor` в `@shemma/domain` (валидный enum → как есть; hex → ближайший по RGB; мусор → fallback); (2) backend `domain/schema/apply.ts` коэрсит цвет на всех путях записи (edge-overlay, set-overlay, set-container-style, set-frame-style) — hex больше не попадает в стор; (3) фронтенд `backfillStoreRecords` коэрсит уже-персистнутые битые `color`/`labelColor` до загрузки, а `loadSnapshot` обёрнут отказоустойчиво: при `ValidationError` снапшот домигрируется, записи, не прошедшие валидацию, отбрасываются (с предупреждением), а очищенный набор переотдаётся штатным `loadSnapshot` (корректно поднимает состояние редактора) — один плохой record больше не уносит всю доску, даже если он невалиден не по цвету. Проверено live-E2E: комната, обнулявшая прежний фронтенд, грузит все шейпы (цвета сведены к палитре); не-цветовой битый record пропускается, доска грузится с остальными.

## 0.31.0 — 2026-06-08 — Feedback telemetry, event-driven text-fit, theme UX + schema-insert & error-code hardening

Крупный feature-батч поверх 0.30.0. Главное: (1) **детерминированная feedback-телеметрия** — сервер пишет действия агента, агент опционально аннотирует, ридер `shemma feedback --diff` показывает дифф «что агент думал vs что вышло» (umbrella DRW-227.01/.02/.03); (2) **событийная авто-обтяжка текста** + явный `shemma_fit_text` (DRW-219/228); (3) **кнопка темы** — клик переключает только светлая⇄тёмная, системная по Opt-Click (DRW-230) + dark mode (DRW-217); (4) **честные коды ошибок** на всех MCP-инструментах — `daemon-unavailable` только транспорт, backend-ошибка = код+статус (DRW-221/229); (5) **hardening AI-insert схем** — elbow-стрелки, growY-aware placement, материализация групп, GC orphan-overlay'ев, MCP-policy «один фрейм = одна связная схема», формат domain-actions для агентов (DRW-205/208-220/222/224-226).

### Fixes

- **Инкрементальные стрелки — elbow.** AI-вставка (`schema-connect` через `patch_schema`) создавала arc-стрелки (кривые), тогда как импортные стрелки схемы — elbow (угловые): визуальный разнобой на одной схеме. `makeArrowShape` в `domain/schema/apply.ts` теперь `props.kind:'elbow'` (паритет с `compile.ts`).
- **Smart-insert учитывает текстовый рост узлов (growY).** Слот для нового узла считался по номинальному размеру шейпа, но tldraw дорастает бокс под многострочный лейбл уже на клиенте → новый узел мог встать внахлёст с выросшим соседом (live-репро: Alert Queue × Svc C3, 140×13px). Теперь occupants учитывают `h+growY`, а новый узел резервирует консервативную оценку высоты лейбла (`estimateEffectiveHeight`, word-wrap модель, откалибрована по живым рендерам).
- **`schema-set-overlay` стилизует существующий узел: полный style-блок.** Раньше overlay-стиль записывался только в `frame.meta.didrawOverlays` и применялся лишь при создании шейпа — «раскрась узел X» через MCP молча ничего не меняло на доске (найдено на живой приёмке). Теперь явный set-overlay обновляет сам шейп; `OverlayEntry` расширен до полного style-блока: `color`, `labelColor`, `fill` (none/semi/solid/pattern), `dash`, `size`, `font` — агенту доступно всё форматирование узла. Целевое действие выражает волю пользователя через агента и применяется всегда (`styleOwnedBy` защищает только от попутных перезаписей: role-пресеты, ре-импорт; к тому же фронтенд штампует `styleOwnedBy:"user"` даже на чисто позиционный drag — гейт по нему блокировал бы перекраску любого двинутого узла).

- **Агентский `shemma_layout` больше не разваливает доску с пинами.** Тройной фикс backend-раскладки (scope=all — путь агентов): (1) восстановление координат pinned-узлов учитывало parent-relative/absolute пространство неверно → пины «прыгали» в мусорные (часто отрицательные) координаты; (2) displacement-пасс (DRW-003) расталкивал ФРЕЙМ от его же pinned-детей (containment ≠ collision) → фрейм улетал, схема разваливалась; (3) фрейм, пересчитанный по компактному ELK-результату, схлопывался вокруг него, оставляя восстановленные пины за рамкой — теперь grow-only покрытие пинов. Плюс policy: MCP-инструкции больше НЕ советуют агенту вызывать layout после правок — раскладка пользователя переживает агентские правки; re-layout только по явной просьбе.
- **`schema-group` материализует контейнеры на доске (DRW-210).** Раньше группировка через `patch_schema` меняла только raw (mermaid): на доске контейнеры не создавались, члены не репарентились — «объедини сервисы в подгруппы» визуально не делал ничего. Теперь reconciliation-пасс в apply: каждая группа нового состояния получает реальный `schema-container` (создаётся при отсутствии — это же лечит legacy-комнаты с группами в raw и плоской доской), члены репарентятся с пересчётом координат (узлы визуально НЕ двигаются), контейнер охватывает членов (grow-only; origin clamp у края фрейма — лейбл не клипуется). Узел, покинувший группу, возвращается во фрейм; контейнер распущенной группы удаляется. Новые parent-relative позиции зеркалятся в `didrawOverlays` (reload-консистентность).
- **Layout зеркалит позиции в `didrawOverlays` — reload показывает ту же доску (DRW-209).** Backend-layout обновлял только shape-записи стора, а `frame.meta.didrawOverlays` оставались старыми; фронтовый hydrate на загрузке страницы применяет `overlay.position` поверх стора → живая WS-вкладка и свежезагруженная страница показывали РАЗНЫЕ раскладки (два источника правды). Теперь `runLayout` тем же батчем мёржит финальные позиции всех didraw-узлов каждого schema-фрейма в overlays: существующие поля записей (color/fill/pinned/styleOwnedBy/…) не теряются, записи создаются и для узлов без них; no-op writeback пропускается (повторный layout остаётся `no-changes`-фикспоинтом). Покрыты все пути layout (`/api/layout`, domain-батчи, MCP `shemma_layout`).
- **Displacement без «кучи»: вытесненные узлы — в ближайшее свободное место (DRW-208).** Старый фолбэк DRW-003 складывал ВСЕ узлы, чьи ELK-позиции пересекали пин, в одну вертикальную колонну справа от пинов — на доске с пинами по всей площади схема превращалась в нечитаемую кучу (live-репро: 21 из 23 узлов на одном x, 7 за рамкой). Теперь ездят только листья (контейнеры кроет coverage-пасс DRW-205), каждый конфликтующий лист ищет ближайший к своей ELK-позиции свободный слот (`findEmptySlot`, окна 3×/6×/12× размера узла → весь регион; occupants: пины, чужие контейнеры, уже осевшие листья; предки/потомки — containment, не collision). Колонна осталась только фолбэком «слота нет нигде». Раскладка стала фикспоинтом: повторный layout → `no-changes`.

### Fixes (продолжение)

- **MCP-policy: один фрейм = одна связная схема.** Инструкции `shemma_patch_schema`/`shemma_create_schema` и agent-overview теперь явно запрещают патчить параллельную НЕСВЯЗНУЮ диаграмму в существующий фрейм (вторая компонента деградирует layout и решения авто-направления — live-находка приёмки, DRW-218): новая несвязная схема → новый фрейм через `shemma_create_schema`.
- **`styleOwnedBy` больше не взводится позиционным drag'ом (DRW-214).** Фронтенд штамповал флаг владения стилем при ЛЮБОМ локальном изменении, включая чистое перемещение — «двигал» ≠ «красил»: style propagation молча переставала применять board-дефолты к любому когда-либо двинутому узлу. Теперь флаг ставится только при style-полях в дельте (color/label). Старые загрязнённые флаги НЕ мигрируются (решение задокументировано): явный set-overlay применяется всегда (DRW-205), загрязнение влияет лишь на bulk-дефолты.
- **Агентский v2-DX: ошибки и описания ведут к правильным инструментам (DRW-224/225/226).** Второй агент, дорабатывая схему, четырежды поставил неверный диагноз (массивы ломают JSON / delete не видит v2 / browser игнорируется в v2 / парсер минимальный) — все мисриды реального-но-другого поведения. Лечение когерентное, errors-as-guidance: (224) v1-`shemma_delete` на v2-цель (`shape:f_…` или узел с `meta.didrawSchemaParent`) теперь возвращает направляющую ошибку «используй `shemma_delete_schema(frameId)` / `shemma_patch_schema(schema-delete-node)`» вместо немого `unknown-ref`; описание `shemma_delete` явно отсылает к v2-инструментам (сами они существовали — это был discoverability, не пробел). (225) storage mermaid-парсер на неподдерживаемую строку (напр. inline `:::class` — classDef/style/edge-labels он как раз поддерживает) даёт ошибку с перечнем поддерживаемого подмножества и рекомендацией `mode:"browser"`; описание импорта разводит browser (полный mermaid, нужен WS) vs storage (урезанный, без WS). (226) первый storage-импорт/`create_schema` необратимо апгрейдит комнату v1→v2 — ответ теперь несёт `upgradedToV2` + `notice` (storage-парсер, delete через `delete_schema`, full mermaid через browser), чтобы переход был осознанным. Покрыто backend (validate/parser/route) + MCP-тестами; live-verified.
- **Группировка через MCP больше не падает с `daemon-unavailable: Failed to parse JSON` (DRW-220).** `shemma_group` и group-действие в `shemma_apply` слали членов в поле `children` (MCP-схема `GroupArgs`), а бэкенд-валидатор и компилятор читали `ids` → `for (const id of a.ids)` по `undefined` бросал необработанный `TypeError`, `validateBatch` в роуте не был обёрнут в try/catch → Hono отдавал 500 `text/plain`, клиентский `r.json()` падал, и MCP маскировал это под «демон недоступен» (хотя демон был жив). Из-за этого агент вообще не мог ставить boundary-рамки вокруг кластеров. Теперь backend принимает `children` как канонический член-лист (алиас к legacy `ids`, нормализация через `groupMembers`), guard превращает отсутствие обоих в структурную validation-ошибку, а `validateBatch` в роуте обёрнут так, что любое исключение валидатора отдаётся структурным JSON, а не plain-text 500. CLI (шлёт `ids`) и обратная совместимость целы. Покрыто backend route/validate/compile-тестами + live-verified через реальный MCP (`shemma_group` и `shemma_apply` создают контейнер; malformed group → чистая 422).
- **Формат domain-действий задокументирован для агентов (DRW-222).** `shemma_apply` объявлял `actions` открытой схемой (`z.record(z.unknown())` → `additionalProperties`), поэтому дискриминатор `kind` и поля каждого действия были невидимы — агент реверс-инжинирил формат серией `dryRun` и промахивался мимо имени поля (так возник `children`/`ids`-баг DRW-220). Теперь описание `shemma_apply` несёт явный перечень всех `kind` (define/connect/group/note/layout/delete) с полями (required/optional), пример JSON-батча и явную заметку «члены группы — в `children`, не `ids`». Workflow-гайд `draw-architecture` получил authoritative-секцию «Action format for `shemma_apply`» (таблица + пример). Лейбл-семантика **проверена живым прогоном**: `\n` в `label`/`text` рендерится как жёсткий перенос (`white-space: pre-wrap`), emoji (variation-selector ⚙️, supplementary-plane 🚀) проходят целиком → follow-up на переносы не нужен. Починен doc-баг: group-пример в гайде использовал legacy `ids` → приведён к `children`. Статический тест пинит наличие маркеров.
- **Генерация больше не складывает узлы в кучу в (0,0) (DRW-223).** Root cause: `compile` создаёт каждый новый шейп в (0,0), а post-apply раскладка (`scope:"affected"`) для ОДНОГО affected-узла = no-op → агент, создающий узлы по одному (`shemma_define` отдельными вызовами) или define-only батчем без рёбер, получал все узлы стопкой в начале координат («механизм распределения» ELK раскидывает только связный батч ≥2 узлов). Новый pass `placeNewFreeShapes` (вызывается в `routes/domain.ts` перед apply) сажает каждый новый свободный geo/note-шейп в свободный слот справа от bbox существующего контента (O(N), без area-scan, чтобы не деградировать на больших комнатах): одиночные/несвязные создания больше не наезжают, а существующие schema-фреймы учитываются как occupants (новые шейпы кладутся рядом, не поверх). Связный батч по-прежнему перераспределяется ELK (placement — безвредный seed). Контракт «AI-правки не авто-перевыравнивают» соблюдён: placement = создание в свободном месте, существующие шейпы не двигаются. Чистая функция (7 unit) + route-E2E (отдельные define не пилятся); backend-сьют 1189 зелёный.
- **Разведение «daemon-unavailable vs backend-ошибка» распространено на все MCP-инструменты (DRW-229).** DRW-221 развёл коды только на пути `applyDomain`; остальные client-методы делали слепой `r.json()` → на non-JSON 500 бросали → MCP-catch → `mapFetchError` → `daemon-unavailable` (та же конфляция на schema-write/import/fit/feedback/layout-selection и read-only GET'ах). Теперь все MCP-backing client-методы (createSchema/patchSchema/setOverlay/duplicateSchema/deleteSchema/importMermaid/fitText/feedback/layoutSelection + getContext/getCanvasView/listRooms/getState) проведены через `result()` (non-2xx аннотируется `httpStatus`, не бросается), а MCP-инструменты (schema-write, domain, read-only, canvas-view) в ветках ошибки мапят через новый guard `isBackendError`→`mapBackendError` (5xx→`unexpected-error`+status, 422→`validation-error`+status); `daemon-unavailable` остаётся только в catch (реальный транспорт). Специфичные коды сохранены (`no-client-connected` по room_url, `import-failed`/`layout-failed` для in-band 2xx, in-band `{ok:false,errors}` остаётся `validation-error`). Read-only GET'ы больше не заворачивают 500-тело как `ok:true data`. Тесты: schema-write (500 JSON + non-JSON → unexpected-error, transport → daemon-unavailable) + read-only (rooms_list/context 500 → unexpected-error, transport → daemon-unavailable).
- **MCP различает «демон недоступен» и «бэкенд вернул ошибку» (DRW-221).** Любой сбой вызова бэкенда из MCP схлопывался в `daemon-unavailable`: `CanvasClient.applyDomain` делал слепой `r.json()`, на non-JSON теле (напр. plain-text 500 от необработанной ошибки) бросал, а MCP-`runActions` ловил исключение и мапил в `daemon-unavailable` → агент видел «демон недоступен», проверял health, хотя демон был жив, а бэкенд вернул 500. Теперь `applyDomain` парсит ответ через хелпер `result()`: полученный non-2xx **не бросается** — тело аннотируется `httpStatus` и возвращается (CLI заодно перестаёт крэшиться на не-JSON 500, получает чистый объект); throw остаётся только для реального reject транспорта (ECONNREFUSED/timeout) → `daemon-unavailable`. MCP `runActions` мапит по статусу (`mapBackendError` → `mapHttpResponse`): 5xx → `unexpected-error`+status, 422 → `validation-error`+status, прочее → `http-error`. Покрыто client-тестами (500/422/200/transport) + MCP-тестами (500→unexpected-error, 422→validation-error, transport→daemon-unavailable). Scope: путь `applyDomain` (репортед-кейс); helper'ы готовы к расширению на schema/read-only пути (follow-up).

### Features

- **Кнопка темы: обычный клик — только светлая⇄тёмная, системная по Opt-клику (DRW-230).** Раньше кнопка темы циклила три состояния (светлая→тёмная→системная→…), и быстрое переключение day/night «застревало» на системной. Теперь обычный клик переключает ТОЛЬКО светлую и тёмную (берёт видимую сейчас схему и флипает — из системной тоже корректно); системный режим не удалён из модели, выбирается **Opt-кликом** (Alt+клик), а пока зажат Opt — иконка кнопки показывает 🌓 и title подсказывает, что клик включит системную. Чистая `toggleColorMode` (unit) + изолированный `ThemeToggleButtonContainer` + `useAltKeyHeld` (re-render на Opt не задевает остальной chrome доски).
- **Ридер feedback — `shemma feedback --diff <room>` + формат файла (DRW-227.03).** Read-only CLI поверх feedback-JSONL (DRW-227.01/.02): для каждой агентской аннотации находит её `request` (точно по `clientOpId`, иначе ближайший предыдущий по `ts`) и печатает пару «claim → actual» (текст/phase аннотации → route/ok/errorCode запроса), помечая `⚠ possible misdiagnosis`, когда `blocker`-аннотация ссылается на фактически успешный запрос. `--json` отдаёт структурированные `entries`; без `--space` — авто-скан `*__<room>.jsonl`. Формат файла (`~/.shemma/feedback/<space>__<room>.jsonl`, оба `kind`, поля, ротация, override `SHEMMA_FEEDBACK_DIR` — уважают и writer демона, и ридер) задокументирован в спеке §13. Строго ридер — никакой аналитики/дашбордов/автозадач. Чистые `parseFeedbackLines`/`computeDiff`/`formatDiff` (9 unit) + команда (4: резолв файла, human/json, no-log). _Завершает umbrella DRW-227._
- **Слой аннотаций агента — `shemma_feedback` (DRW-227.02, opt-in).** Поверх костяка DRW-227.01: новый MCP-инструмент `shemma_feedback` (`text`, опц. `phase` intent/blocker/resolution, опц. `clientOpId`) → роут `POST /api/agent/feedback` дописывает `kind:annotation` в тот же room-JSONL. Аддитивно: костяк работает и без единой аннотации; при выключенном флаге роут — no-op `{ok:true,recorded:false,reason:"feedback disabled"}`, вызов агента не падает; инструмент НЕ трогает доску. Запись несёт ts/space/room/clientOpId?/phase?/text(усечён)/agent?/sessionId?(из env MCP). Роут вне allowlist костяка (сам annotation-POST не логируется). Ключевая ценность — офлайн-джойн аннотации к `request`-записи по `clientOpId`/таймлайну = дифф «что агент думал vs что вышло». Нудж в описании tool'а + секция «Recording feedback» в `draw-architecture.md`. Чистая `buildAnnotationRecord` (4 unit) + route-integration (annotation рядом с request; off→recorded:false; пустой text→400) + MCP 5 кейсов. Follow-up: DRW-227.03 (формат+ридер `shemma feedback --diff`).
- **Объективный костяк feedback-телеметрии (DRW-227.01, opt-in).** По дизайн-спеке DRW-227: при включённом флаге `SHEMMA_FEEDBACK=1` демон детерминированно пишет в append-only JSONL (`~/.shemma/feedback/<space>__<room>.jsonl`) запись `kind:request` на каждый агентский вызов — мутации (`/api/domain`, `/api/layout`, `/api/agent/{layout-selection,import-mermaid,fit-text,style-apply}`, `/api/schema/*`) + чтение `/api/agent/context`. Запись несёт нормализованный route, метод, space/room, clientOpId, тайминг, httpStatus, ok, errorCode и size-bounded payload/result (большие тела → `__truncated`/`__omitted`). Реализация — Hono-middleware на `/api/*` (ставится только при флаге, после bundle-resolver), best-effort (сбой логирования не влияет на ответ агенту), single-level ротация по размеру. **Off by default** (лог содержит реальный контент схем — приватность). Цель — офлайн-анализ поведения агентов и дифф «что агент думал vs что вышло» (DX-фиксы по данным, а не догадкам — питает пласт DRW-221/224). Чистые `normalizeRoute`/`isLoggedRoute`/`boundValue`/`extractOutcome`/`buildRequestRecord` (15 unit) + `FeedbackWriter` (4 unit) + integration через `makeApp` (мутация логируется, `/api/state` нет, без флага файла нет). Слой аннотаций (`shemma_feedback`) и ридер — follow-up DRW-227.02/.03.
- **Обтяжка текста — команда подгонки размера под содержимое (DRW-219).** Объекты с текстом (geo/note) часто имеют неудачные габариты: узкая высокая колонна, широкий бокс с пустотой или обрезка. Новая команда «Обтянуть текст» (кнопка в панели выделения + ⌘⇧F, настраиваемый) подбирает оптимальную ширину и минимальную высоту: бинарный поиск по ширине (`pickOptimalWidth`, измерение реальной высоты текста через `ShapeUtil.onBeforeCreate` с базовой `h=1`, чтобы growY отражал полную нужную высоту, а не «текст влез в большой бокс») — короткий текст становится узким плотным боксом, длинный сбалансированно переносится в пределах читаемой ширины (≤400px), высота сжимается до минимума без обрезки. Применяется к каждому выделенному текстовому объекту независимо; объекты без текста и нетекстовые типы не трогаются. Новый размер помечается user-owned (`meta.didrawSizePinned`) — AI-layout его не перетирает; позиция не меняется. Pure-функция `pickOptimalWidth` (6 unit-тестов) + live-verified (узкая колонна 359→122px, простыня 700→366×92, короткое слово → плотный бокс; пустой объект нетронут).
- **Авто-обтяжка по событию + `shemma_fit_text` (DRW-228).** Обтяжка DRW-219 теперь вызывается автоматически в нужный момент, а не только вручную: geo/note подгоняется под текст **один раз** — когда объект добавлен (включая AI/remote-создание через MCP) или когда правка текста завершена (по выходу из редактирования, не на каждой клавише). Так к моменту раскладки узлы уже несут корректные габариты, а ручной ресайз пользователя (`didrawSizePinned`) не перетирается. Реализация — событийная: store-listener `{source:'all'}` ловит add/text-change (мид-набор откладывается, фитится на editing-end через `react(getEditingShapeId)`); петли нет (фит пишет w/h/meta, не richText → `textChanged=false`). Дополнительно — явный MCP-инструмент `shemma_fit_text` (WS-мост по образцу `import-mermaid`, нужна открытая вкладка — текстовые метрики только в браузере): agent-invokable пасс «обтянуть/подтвердить» перед раскладкой или чтобы покрыть любой узел, который live-фиттер не зацепил; `targets?` (shape ids / didrawNames; без них — все подходящие); уважает size-pin (user-размеры и уже обтянутые пропускаются → `count` отражает только реально изменённые); на `no-client-connected` возвращает `room_url` для ретрая. Live-verified: AI-define при открытой вкладке приходит обтянутым (`sizePinned:true`, 324×181 vs дефолт 220×80), вкладка фитит и состояние на загрузке, `shemma_fit_text` round-trip ok. Извлечён `applyTextFitToShape` (одиночная обтяжка) поверх DRW-219. Чистые `shouldAutoFit`/`textChanged` (12 unit); WS-мост покрыт по всем слоям (backend route + ws-protocol parse + WsHub, frontend ws-handler, MCP tool 6 кейсов).
- **GC orphan-overlay'ев (DRW-216).** `didrawOverlays` держал записи удалённых узлов вечно (keep-dead — чтобы re-add того же nodeId вернул пользовательский вид); на долгоживущих AI-редактируемых досках это растило файл комнаты (вопрос приёмки DRW-205 «зачем остаются записи?»). Добавлена age-based generation GC: каждый apply-проход фрейма = поколение (`frame.meta.didrawOverlayGen++`); orphan метится поколением смерти (`OverlayEntry.deadGen`), при воскрешении узла метка снимается. Сборка случается ТОЛЬКО при превышении порога (orphans > 2× живых узлов И ≥ 50) и только для orphan'ов, переживших 20 поколений — недавние удаления остаются восстановимыми (keep-dead для re-add сохранён). Малые/обычные доски не затрагиваются. Чистая функция `gcOverlays` (10 unit-тестов) + live-verified: 58 orphans собраны после 20 проходов, живые узлы и недавний re-add нетронуты.
- **Форма/прозрачность узлов + стили контейнеров и фрейма (DRW-215).** Node-overlay получил `geo` (форма: ellipse/diamond/cloud/hexagon/star/… — tldraw geo enum) и `opacity` (0..1, record-level): set-overlay ремоделирует живой узел и seed'ит новый; hydrate применяет оба поля на reload. Новые действия: `schema-set-container-style {name, style:{color/fill/dash/titlePosition}}` — стилизация контейнера группы по имени (включая материализованный тем же батчем); `schema-set-frame-style {style:{color/label}}` — цвет рамки и display-имя фрейма. MCP-схема/доки обновлены.
- **define с позицией одним патчем (DRW-213).** `schema-define` + `schema-set-overlay{position}` в одном батче ставит новый узел ровно в указанную точку — in-batch overlay теперь виден созданию узлов (раньше читалось состояние «до» батча, и smart-insert перебивал явную позицию). `pinned:true` в том же батче — узел закреплён с рождения; frame-fit покрывает явно позиционированный узел за краем рамки.
- **Не-схемные шейпы: adopt в схему + объектное удаление (DRW-212).** `schema-adopt-shape {shapeId, role, label?, nodeId?}` — нарисованная руками фигура становится узлом схемы IN-PLACE: получает didraw-identity/role/label (текст обновляется только у текстонесущих шейпов), появляется в raw, сразу адресуема (connect в том же батче вяжет стрелку к усыновлённому шейпу); позиция/размер/вид не меняются. `schema-delete-shape {shapeId}` — удаление не-схемного шейпа с каскадом биндингов и висячих стрелок (для didraw-узлов — `schema-delete-node`). Ошибки `unknown-shape`/`not-adoptable`.
- **Стилизация рёбер: `schema-set-edge-overlay` (DRW-211).** Ребро адресуется объектно направленной парой from→to; полный style-блок стрелки — `color`, `labelColor`, `dash` (draw/dashed/dotted/solid), `size`, `font`, `kind` (arc/elbow), `arrowheadStart/End`, `label`. Применяется к живой стрелке сразу и персистится в `frame.meta.didrawEdgeOverlays` (merge-семантика; re-add ребра подхватывает сохранённый стиль). Ограничение: модель держит ОДНО ребро на пару from→to — параллельные дубли стрелок перекрашиваются вместе. MCP-схема и инструкции patch_schema обновлены.
- **Тёмная тема (DRW-217).** Board-level переключатель 🌓 в шапке доски (цикл светлая → тёмная → системная; иконка отражает режим): tldraw canvas/UI переключается через `updateUserPreferences` без reload (initial `colorScheme` — стабильный prop, чтобы смена темы не пересоздавала editor и не схлопывала открытую панель настроек — находка приёмки), режим `system` следует OS-теме live (matchMedia). Выбор хранится per-browser в `localStorage shemma:theme` (в canvas state/room store НЕ пишется) и применяется до первого рендера (без flash). Все кастомные поверхности shemma темизированы через CSS-переменные `--shemma-*` (`:root` / `html[data-shemma-theme=dark]`): design-tokens переведены на `var()` (26 компонентов — панели, кнопки, popover'ы, модалки, prompts, gallery, spaces — подхватили автоматически), `settings/styles.css` переведён с hardcoded hex на переменные, точечные inline-цвета (Gallery/RoomCard/RoomListRow/NewRoomForm) заменены на токены. SchemaContainerShape уже был theme-aware (`useColorMode`) — проверен в dark. Gallery/Spaces (страницы без editor) темизируются тем же контроллером.
- **Видимая кнопка панели настроек ⚙️ в шапке доски (DRW-206).** Панель настроек (DRW-179) открывалась только по Option+RightClick / ⌘⇧P — недискавериваемо. Теперь постоянная кнопка ⚙️ в верхней зоне (перед кнопкой темы 🌓 и Prompts 💬) открывает/закрывает панель в актуальном контексте (board-панель без выделения, selection/node при выделении — общий `toggle()` с ⌘⇧P); повторный клик и Escape закрывают; Option+RightClick без регрессий; `aria-pressed` отражает состояние. Кнопки шапки (⚙️ 🌓 💬) приведены к единому виду и размеру через общий `ChromeButton` (emoji-иконки, паритет с нативной tldraw-зоной) — находка приёмки.
- **Тип стрелок (arc/elbow) в панели настроек (DRW-207).** Board-уровень: ряд «Стрелки → Тип» в панели «По умолчанию» задаёт тип НОВЫХ стрелок — и ручных (tldraw arrow tool через `ArrowShapeKindStyle`), и AI/domain-путей (`patch_schema` connect, `/api/domain` connect/note, mermaid-import); default персистится в `room.meta.styleDefaults.arrowKind` и переживает reload. Tri-state: re-click активной кнопки снимает default → статус-кво (ручные arc, AI elbow). Selection-уровень: при выделении, содержащем стрелки (включая потомков выделенных фреймов/контейнеров), панель выделения переключает `props.kind` уже существующих стрелок — только стрелок, с сохранением биндингов, лейблов и прочих стилей; sticky-meta контейнеров для arrowKind не пишется (board-level only). Выбор kind в нативной style-панели tldraw синхронизируется в board default (паритет с dash/font/size). Ограничение: live cross-client push дефолтов по-прежнему ждёт DRW-182 (broadcastRoomMeta no-op) — паритет с остальными board defaults.
- **Grow-only frame-fit.** После каждого apply фрейм дорастает вниз/вправо, чтобы покрыть effective-габариты детей: переименованный в длинный лейбл узел больше не вылезает за рамку (не клиппится). Дети при этом не двигаются — совместимо с «layout только по пользователю».

### Tests

- +9 тестов apply (elbow, growY-occupants, wrap-reserve, anchor-bias, frame-fit, container-resolver edge-cases: 2-уровневая вложенность, cross-frame reject, disconnect вложенного эндпоинта) + 2 теста `findEmptySlot` anchor + тест анти-колонны (`layout-pinned-frame.test.ts`: пины-«одеяло» + 8 узлов цепочкой → нет ≥4 на одном x, нет нахлёстов, всё в рамке, детерминизм).
- Задокументировано ограничение: при expansion фрейма BT/RL узел добавляется с положительного края (вниз/вправо) — отрицательных координат не возникает; смысловой край BT/RL при расширении не учитывается.

**Tests:** `bun run test` (domain 131 / backend 1120 / client 47 / cli 218 / mcp 246) + frontend 568 — все зелёные.

## 0.30.0 — 2026-05-31 — Schema AI-insert fixes (container-nested connect, user-triggered layout, smart-insert)

Исправления объектного редактирования v2 schema-фреймов агентом через MCP: связи к узлам внутри контейнеров теперь материализуются, AI-правки больше не перевыравнивают/схлопывают схему, новый узел вставляется в свободное место с авто-расширением фрейма.

### Fixes

- **Связи к узлам внутри контейнеров (subgraph).** `shemma_connect` / `schema-connect` к узлу, лежащему в schema-container, молча терял стрелку: резолвер эндпоинтов (`apply.ts` `findShapeByNodeId`/`extractExistingNodeIds`) искал только прямых детей фрейма. Теперь резолвер обходит всё поддерево фрейма → arrow + bindings создаются для любого вложенного эндпоинта.
- **AI-правки не перевыравнивают схему (layout — только по пользователю).** Убран автоматический re-layout на каждой инкрементальной AI-вставке: post-apply `runLayout` в `patch_schema` и re-layout в `measured-bounds`. Раньше это репозиционировало ручную раскладку пользователя и **схлопывало фрейм с контейнерами** до размера нового блока. Раскладка теперь запускается только пользователем (`⌘⇧L`) или явным `shemma_layout`. `measured-bounds` по-прежнему применяет измеренные размеры (текст влезает), но узлы не двигает.

### Features

- **Smart-insert размещение.** Новый узел при AI-вставке ставится в свободное место внутри фрейма (`findEmptySlot`); если слот не влезает — фрейм расширяется (`computeExpansion`) и узел кладётся на новый край, **не сдвигая существующие узлы** (DRW-178 helpers подключены к `patch_schema`).
- **MCP-инструкции: object-first редактирование.** `shemma_get_instructions` (overview) + описание `shemma_patch_schema`: читать живой граф (`shemma_canvas_view`), править объектными вербами, не трогать raw mermaid (вспомогательный производный срез), layout — по запросу.

**Tests:** backend 1108 + MCP 246 зелёные (TDD RED→GREEN). Live-verified на доске (chrome-devtools): связи к вложенным узлам, отсутствие схлопывания фрейма, smart-insert размещение + авто-рост фрейма.
**Follow-ups (Backlog):** DRW-204 (board-canonical: доска = source of truth, mermaid = производная), + проверочная задача (re-test/дополнительные работы).

## 0.29.0 — 2026-05-31 — Autolayout rebuild + UX/bugfix batch

Крупный **autolayout-rebuild** (pluggable placement + arrow routing) и **UX/bugfix-батч** (10 задач + polish). Релиз также включает подготовку репозитория к публикации как open-source: MIT license, agent-agnostic документация (README/CLAUDE/AGENTS), анонимная установка для публичного репозитория (`curl … | sh`).

### Autolayout rebuild

Полностью переработанная авто-расстановка схем (`⌘⇧L`):

- **Авто-выбор направления.** Для незакреплённого фрейма движок сам подбирает направление раскладки, дающее самую компактную схему (квадрат вместо длинной полосы) — вместо фиксированного TB/LR.
- **Flow-aware ориентация контейнеров.** Контейнер с внутренним потоком (рёбра между своими узлами) раскладывается вдоль фрейма; контейнер-«пучок» без внутренних рёбер — перпендикулярно.
- **Меньше пересечений стрелок.** Порядок узлов внутри контейнера автоматически зеркалится для распутывания связей; маршрут стрелок оптимизируется по реальной геометрии (obstacle-pass по midpoint).
- **Per-frame движок раскладки** (layered / mrtree / …) с наследуемой политикой направления.
- **AUTO-режим «•».** Кнопка «•» на фрейме сбрасывает закреплённое направление и возвращает компактнейшую авто-раскладку; cardinal-кнопки (TB/LR/RL/BT) закрепляют направление вручную.
- Явно выбранное направление контейнера/фрейма уважается и не перетирается при ре-лэйауте.

### UX/bugfix batch (10 задач + polish)

- **Room tags.** Тэги комнат `{name, color}` (7 цветов), хранятся в `RoomMeta.tags` (envelope `meta.tags`). `PUT /api/rooms/:id/tags` — идемпотентная замена массива с валидацией (непустое имя ≤24, цвет из палитры, case-insensitive dedup, ≤12); `GET /api/rooms` отдаёт `tags`; client `setRoomTags`. UI: цветные чипы на карточках и строках списка + click-to-filter (AND-логика) + inline-popover редактор (имя + 7 свотчей). `apps/backend/src/{types,room-tags,routes/rooms}.ts`, `apps/frontend/src/gallery/{tag-colors,tag-filter,TagChip,TagEditor,RoomTagsRow}.tsx`.
- **Gallery grid/list view.** Глобальный тумблер Grid↔List (localStorage `shemma:gallery-view-mode`), новый компонент `RoomListRow`; в списке клик по всей строке (кроме кнопок/тэгов/inline-input, гард `closest("button,a,input")`) открывает комнату. Общие per-room хендлеры вынесены в `use-room-actions.ts`.
- **Configurable keyboard shortcuts.** Реестр 8 команд (`registry.ts`) + localStorage-оверрайды (`config.ts`) + чистый `matchShortcut`/`bindingFromEvent`/`formatBinding` (`match.ts`); `getActiveBinding` читается в момент события → remap применяется без ре-регистрации listener'ов. Панель «Горячие клавиши» (`ShortcutsPanel.tsx`) из board-панели: remap-capture, conflict-warn, reset/«Сбросить всё». Binding-модель: один флаг `mod` = Cmd-или-Ctrl (кроссплатформенно).
- **Copy-link shortcut → `⇧⌘S`.** Дефолт `⌘⌥C` не срабатывал: на macOS Chrome это акселератор «Inspect Element», браузер съедает keydown до страницы (CDP-инъекция это обходит — поэтому автоматическая проверка не ловила баг). Перенесён на `⇧⌘S` (Share); переназначаем через панель. Контекст-меню-хинт «Copy link» теперь динамический (`formatBinding(getActiveBinding(...))`).
- **Reveal room file in Finder.** Бейдж «room: …» на доске стал кликабельным → `POST /api/rooms/:id/reveal` (`flushIfDirty` → `findRoomFile` → reveal: macOS `open -R`, Windows `explorer /select,`, Linux — папка) показывает `.json`-файл комнаты в файловом менеджере. Client `revealRoom`.
- **Gallery auto-refresh.** Список комнат перезапрашивается на window `focus` / `visibilitychange→visible` + poll 13s (coalesce 4s) → превью (server-rendered SVG с `?v=version`) больше не устаревают; раньше версия не обновлялась без ручного refresh.
- **Container fill for styled subgraphs → `solid`.** `resolveSubgraphStyle` маппил явный Mermaid `style ... fill:` в `"semi"` → теперь `"solid"` (pastel). Дополняет DRW-189 (0.28.0): тот покрыл default-контейнеры и `makeSchemaContainerShape`, этот — путь стилизованных субграфов (`apps/backend/src/routes/schema.ts:~233`). Существующие комнаты не мигрируются.
- **Layout-engine buttons в одну строку** (settings popover, `settings-section__row--layout-engine` + `flex-wrap:nowrap`).
- **Убраны 3 рудиментарных тумблера** (Авто-направление / Равномерно / По центру) — мертвы после autolayout-rebuild (frontend сам выбирает направление и оптимизирует midpoints). Удалён только UI + wiring; домен-типы `LayoutParams` и дефолты (`autoDirectionEnabled:true`, `midpointDistribution:"even"`) и backend-консьюмеры сохранены.
- **Storage hygiene (T4 аудит).** Подтверждено: мёртвый `meta.didrawIsGroup` уже удалён ранее (DRW-148); legacy `meta.role` ещё читается (миграция отложена → DRW-195). Действий в коде не потребовалось.
- **UI polish.** Кнопка пути пространства в галерее приведена к единому бейдж-стилю (рамка + фон + hover); hover-подсветка у «← Gallery»; убраны подчёркивания имён комнат и бейджа; лейбл force-relayout сокращён до «Принудительно».

**Tests:** 569 frontend + backend (room-tags / envelope / routes-rooms / schema) зелёные; frontend `tsc` чист.
**Follow-ups (Backlog):** DRW-195 (role→didrawRole reader-миграция), DRW-196 (превью через WS-push вместо poll), DRW-197 (дочистка мёртвых `LayoutParams`-полей).

---

Релизы 0.28.0 и старше — в [docs/changelog-archive.md](docs/changelog-archive.md).
